networks:
  appnet:
    external: true

services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:5.2.1
    container_name: phpmyadmin
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql
    mem_limit: 128m
    restart: always
    networks:
      - appnet
